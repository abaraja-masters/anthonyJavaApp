import static org.junit.jupiter.api.Assertions.assertEquals;

import org.junit.Test;

/**
 * This is my unit testing file, so far it's just hard coded test case, it's just practice.
 */
public class TestApp {
    @Test
    public void test(){
        assertEquals(2, 1+1);
    }
}
